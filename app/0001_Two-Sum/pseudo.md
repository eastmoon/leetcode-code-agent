# Two Sum 虛擬碼

函式 尋找兩數之索引(數字清單, 目標值)
    建立 空對映 數值對應索引表
    迴圈 走訪 數字清單，取得 目前索引 與 目前數值
        設 所需數值 為 目標值 減去 目前數值
        若 所需數值 存在於 數值對應索引表 中 則
            回傳 由 數值對應索引表中所需數值對應之索引 與 目前索引 組成的清單
        否則
            將 目前數值 與 目前索引 的對應關係 加入 數值對應索引表
        結束若
    結束迴圈
    回傳 空清單
結束函式
